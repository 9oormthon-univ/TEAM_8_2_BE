server:
  port: 8092

spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: jdbc:mysql://43.202.89.97:55359/clouddog?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    open-in-view: false

logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.type: trace

FIREBASE_KEY: ./serviceAccountKey.json